spring.application.name=autismo-app

# Configuração do banco de dados PostgreSQL (Render)
spring.datasource.url=dpg-d016udfgi27c73a1q24g-a
spring.datasource.username=funny_rbyb_user
spring.datasource.password=diqa2T2WPrVVg48ORJBk51WQXxgMoQWd
spring.datasource.driver-class-name=org.postgresql.Driver

# Quando terminar de configurar coloque assim:
# spring.datasource.url=${DB_URL}
# spring.datasource.username=${DB_USERNAME}
# spring.datasource.password=${DB_PASSWORD}

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Porta da aplicação
server.port=8080
